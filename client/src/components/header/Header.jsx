import MainTitle from '../main-title/MainTitle';
import { StyledHeader } from './header.styles';

const Header = () => {
	return (
		<StyledHeader>
			<MainTitle>Users Dashboard</MainTitle>
		</StyledHeader>
	);
};

export default Header;
